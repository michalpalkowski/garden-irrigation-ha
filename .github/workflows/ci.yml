name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  hacs-integration:
    name: HACS integration checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: JSON checks
        run: scripts/ha-integration-release-check.sh
